{
  "name": "cal",
  "version": "1.0.0",
  "description": "Calendar printer",
  "author": "Paul Sobolik",
  "license": "ISC",
  "main": "cal.js",
  "scripts": {
    "start": "node cal.js",
    "build": "npm run init && npm run package",
    "lint": "standard",
    "init": "npm install",
    "package": "grunt copy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "argparse": "^1.0.7"
  },
  "devDependencies": {
    "grunt": "^1.1.0",
    "grunt-contrib-copy": "^1.0.0",
    "standard": "^14.3.3"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/psobolik/cal.git"
  }
}
